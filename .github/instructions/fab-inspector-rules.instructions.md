---
description: Fab Inspector rule authoring, JSONLogic patterns, operators, part iterators, and patch construction.
---

- Design rules to return an array of failing items with an expected result of `[]` for actionable output.
- Use `let` to avoid duplicate API calls or expensive operator evaluations.