{
  "$schema": "../../.ai-assets/RuleSchemas/fab-inspector-rules.schema.json",
  "rules": [
    {
      "id": "EXECUTE_SQL_QUERY_WITH_PARAMETERS",
      "name": "Execute SQL query (explicit parameters)",
      "description": "",
      "itemType": "none",
      "disabled": false,
      "test": [
        {
          "sqlquery": [
            "SELECT COUNT(*) as CityCount FROM [dbo].[dimension_city]",
            "{context-fabricworkspace}",
            "{context-fabricitem}",
            true,
            true
          ]
        },
        [
          {
            "CityCount": 116295
          }
        ]
      ]
    }
  ]
}
